# Next Starter

```bash
# adding jest for unit testing

yarn add -D jest jest-environment-jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom @types/jest ts-node

yarn create jest@latest
```
